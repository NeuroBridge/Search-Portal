import styled from 'styled-components'
import { Button } from '../button'

export const QuerySubmitButton = styled(Button).attrs({
  children: 'Submit Query'
})`
`

export const QueryResetButton = styled(Button).attrs({
  children: 'Reset'
})`
  background-color: #724;
`
